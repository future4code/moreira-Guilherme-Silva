/* Exercícios de interpretação de código

1.As mensagens impressas no console serão:

(saída do console)
a. false
b. false
c. true
d. boolean

2. Os números digitados serão contatenados, e não somados. Isso porque ao utilizar o prompt, por padrão o tipo das variáveis serão strings
Ex: caso seja digitado 10 e 20
(saída do console)
1020

3. Para corrigir o problema do ex anterior, basta fazer a transformação de string para números através do Number:

let primeiroNumero = Number(prompt("Digite um numero!"))
let segundoNumero = Number(prompt("Digite outro numero!"))

const soma = primeiroNumero + segundoNumero

console.log(soma)
*/

